make directories build/windows and build/linux first.

For windows:
    -- cd build/windows
    -- cmake ../../
    -- cmake --build .
    
for linux:
    -- cd build/linux
    -- cmake ../../
    -- cmake --build .